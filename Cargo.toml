[workspace]
resolver = "2"
members = [
    "crates/cli",
    "crates/focus",
    "crates/proc_macros",
    "example"
]

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[profile.dev.package."*"]
opt-level = 2
